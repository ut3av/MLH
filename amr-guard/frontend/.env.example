# Backend API base URL (defaults to http://localhost:8000 when running locally)
# For production (e.g. Render / Railway / Cloud Run), set this to your backend domain:
# VITE_API_BASE=https://diya-backend.onrender.com
VITE_API_BASE=http://localhost:8000
